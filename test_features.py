from features import extract_features  # features.py file path irukkanum

# Example URL to test
url = "http://badbank-login.com"

# Extract features
features = extract_features(url)

# Print features to see
print(features)